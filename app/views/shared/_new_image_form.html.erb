<%= turbo_frame_tag @image do %>
  <%= simple_form_for @image do |f| %>
    <%= f.input :name %>
    <%= f.input :image, as: :file %>
    <%= f.collection_select :vision_board_id, @vision_boards, :id, :title %>
    <%= f.button :submit %>
  <% end %>
<% end %>
