<h1><%= @vision_board.title %></h1>

<% unless @images.empty? %>
  <% @images.each do |image|  %>
    <%= image_tag url_for(image.vision_image.variant(resize_to_limit: [300, 300])) %>
    <br>
    <%=  image.image_name%>
  <% end %>
<% else %>
  <p>No images, add them!</p>
<% end %>
